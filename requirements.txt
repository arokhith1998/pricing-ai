# Core (Phase 1 — CSV diagnostic). Open-source + local first.
pandas>=2.2
numpy>=1.26
# Dashboard
streamlit>=1.36
altair>=5.3
# Phase 2 — win-probability model + native SHAP explanations
lightgbm>=4.3
scikit-learn>=1.4
# LLM assist layer (narrative + CSV column mapping). Provider-swappable.
openai>=1.40
anthropic>=0.40
# API (FastAPI wrapper consumed by the Next.js front end)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
# Tests
pytest>=8.0
